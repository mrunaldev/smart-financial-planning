================================================================================
  SmartFin - Improvement TODO List
  Generated: 2026-07-31
================================================================================


[P0] CRITICAL - Data Integrity & Security
================================================================================

[x] Use crypto.randomUUID() instead of Date.now() for entry IDs     ── v2.0
    - Replaced String(Date.now()) with crypto.randomUUID()
    - Fixed in readSectionFormEntry() and saveEmergencyFundFromForm()

[x] Use Firestore merge writes instead of full document overwrite    ── v2.0
    - Changed .set(appData) to .set(appData, { merge: true }) in doSave()
    - Prevents last-write-wins data loss across multiple tabs

[x] Add auto-backup before destructive operations                    ── v2.0
    - autoBackup() helper exports JSON before Import and Reset
    - Automatic download of smartfin-auto-backup-{date}.json

[x] Fix firestoreInitialized flag (never set to true)               ── v2.0
    - Set window.firestoreInitialized = true after Firestore listener init
    - Logger Firebase write path now active (no longer dead code)

[x] Scope app_logs to per-user collection path                      ── v2.0
    - Changed from global app_logs to users/{userId}/logs subcollection
    - Reads and writes scoped to authenticated user only
    - Removed userId filter in getLogs() (redundant with per-user path)

[x] Trim text inputs on save                                        ── v2.0
    - readSectionFormEntry() already uses .value.trim() for text fields
    - saveEmergencyFundFromForm() also trims
    - Auth form (email, name) also trimmed


[P1] HIGH PRIORITY - UX & Features
================================================================================

[x] Replace alert()/confirm()/prompt() with custom modals          ── v2.0
    - 67 blocking dialog calls replaced with async custom modals
    - modules/modal.js: showAlert, showConfirm, showPrompt, showTypedConfirm, showToast
    - All functions async-safe, keyboard accessible, themed

[x] Add "Forgot Password" / Password Reset flow                    ── v2.0
    - Uses Firebase sendPasswordResetEmail()
    - "Forgot password?" link on sign-in form
    - Friendly error messages for invalid/missing email

[x] Add loading spinner during auth and initial data fetch          ── v2.0
    - Full-screen spinner overlay on page load
    - Removed in onAuthStateChanged callback
    - Auth screen starts hidden, revealed after spinner

[x] Add Dashboard / Overview tab                                    ── v2.0
    - Full dashboard: budget status, net worth, goal progress, accounts
    - modules/dashboard.js with renderDashboard()
    - Default landing tab for onboarded users

[x] Add spending trend charts across months                         ── v2.0
    - 6-month bar chart (income/expenses/savings) on dashboard
    - Chart.js lazy-loaded on demand

[x] Add recurring payment reminders/notifications                   ── v2.0
    - In-app banner on dashboard showing recurring outflows
    - Lists up to 8 upcoming payments with frequency and amount

[x] Add input validation improvements                               ── v2.0
    - Email regex + password length validation on auth form
    - Name length validation on registration
    - CSS :invalid pseudo-class styling for inputs

[x] Add Undo for delete operations                                  ── v2.0
    - Toast notification with "Undo" action button (5-second window)
    - Restores entry to original tab on undo click


[P2] MEDIUM PRIORITY - Performance & Code Quality
================================================================================

[x] Debounce search input re-renders                               ── v2.0
    - 300ms debounce on searchInput "input" event

[x] Stop re-rendering on theme toggle                              ── v2.0
    - Theme toggle no longer calls full render()
    - Only re-renders chart if on budget tab

[x] Debounce pie chart recreation in budget edit mode               ── v2.0
    - PIE_CHART_DEBOUNCE_MS constant defined (500ms)

[x] Lazy-load SheetJS and Chart.js                                 ── v2.0
    - loadScript() helper for on-demand script loading
    - SheetJS loaded on first Excel export
    - Chart.js loaded on first chart-tab visit

[x] Deduplicate add*Entry functions                                 ── v2.0
    - Already refactored to upsertSectionEntry / readSectionFormEntry pattern

[x] Deduplicate variable expenditure calculation                    ── v2.0
    - Extracted to calcVariableExpenditure() shared helper
    - Used by both applyMonthlyAutoValues() and Quick Update handler

[x] Extract magic numbers to named constants                        ── v2.0
    - modules/constants.js: 25+ named constants
    - Timing, queue limits, UI, financial, color constants

[x] Encapsulate window.* budget globals                             ── v2.0
    - budgetState module-scoped object defined
    - Replaces window._budget* / window._mismatch* pattern

[x] Consider splitting app.js into modules                          ── v2.0
    - app.js converted to type="module"
    - modules/constants.js, modules/modal.js, modules/dashboard.js
    - Further splitting possible in future iterations


[P3] NICE TO HAVE - PWA, Testing, DevOps
================================================================================

[ ] Add PWA support (manifest.json + service worker)
    - App already has mobile-web-app-capable meta tags
    - Add manifest.json for installability
    - Add service worker to cache HTML/CSS/JS for offline use
    - Enable Firestore offline persistence

[ ] Add email verification on registration
    - Call user.sendEmailVerification() after account creation
    - Show banner until email is verified

[ ] Add re-authentication dialog before account deletion
    - Currently tells user to "log out and back in"
    - Prompt for password inline instead

[x] Auto-detect system theme preference                             ── v2.0
    - getDefaultTheme() checks localStorage then prefers-color-scheme
    - OS change listener auto-updates if no manual override saved

[x] Add prefers-reduced-motion support                              ── v2.0
    - @media (prefers-reduced-motion: reduce) disables all transitions/animations
    - Covers spinner, toast, modal, panels, progress bars

[ ] Improve accessibility (ARIA roles, focus management)
    - Add role="tab", aria-selected, tabindex to tab buttons
    - Move focus to panel content on tab switch
    - Add skip-to-content link
    - Add focus-visible styles for keyboard navigation

[ ] Update cache-busting strategy
    - Current: static ?v=2 on script tags (manually updated)
    - Use build number from APP_VERSION: ?v=${build}

[x] Fix stale test.html                                             ── v2.0
    - Updated DEFAULT_TABS stub to 11 tabs with v2 IDs + semantic/core properties
    - Fixed all tab order/ID assertions
    - Added 21 new tests: crypto.randomUUID, autoBackup, constants, debounce
    - Updated manual checklist with v2 features (67 total assertions)

[x] Add CI pipeline steps                                           ── v2.0
    - .github/workflows/ci.yml: lint, lighthouse, html-validate jobs
    - .eslintrc.json: browser + ES2022 + module config with Firebase globals
    - .github/lighthouserc.json: perf >= 70, a11y >= 80, best-practices >= 80

[ ] Add SIP calculator / what-if analysis tool
    - Project investment growth with compound interest
    - "What if I invest X more per month?" scenarios

[x] Add PDF report export                                           ── v2.0
    - exportToPdf() generates styled HTML table in a new window
    - Browser print-to-PDF with landscape layout, auto-close after print
    - "Export PDF" button added to toolbar alongside "Export Excel"

[ ] Add session timeout for shared devices
    - Firebase tokens auto-refresh indefinitely
    - Add configurable idle timeout (e.g., 30 min)

[ ] Firestore document size monitoring
    - Single document per user can grow large over years
    - Add check/warning when approaching 1MB limit
    - Plan: migrate monthly budget data to sub-collection if needed


================================================================================
  Priority Key:
    P0 = Critical (data loss/security risk, fix ASAP)
    P1 = High (significant UX impact, do next)
    P2 = Medium (code health, performance)
    P3 = Nice to have (polish, future features)

  Mark completed items with [x]
================================================================================
